package application.models;

// and interface which is to be used by custom UI elements
public interface IUpdateable {

	// abstract method which is to be used to update the UI element
	abstract void update();
}
